# This file is no longer the source of truth for nginx configuration.
#
# Nginx config has been moved to the shared reverse proxy on le-projects-01:
#   /root/proxy/nginx/conf.d/risingcompass.conf  (RC server blocks)
#   /root/proxy/nginx/conf.d/chadrising.conf     (WP server blocks)
#   /root/proxy/nginx/conf.d/00-acme.conf        (HTTP→HTTPS + ACME)
#   /root/proxy/nginx/ssl-common.conf            (shared SSL settings)
#
# To edit: SSH to root@138.197.111.66, edit the files above,
# then: cd /root/proxy && docker compose exec nginx nginx -s reload
