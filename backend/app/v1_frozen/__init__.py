"""V1 frozen calibrator and rubric — bit-for-bit pinned to commit a839df9.

DO NOT MODIFY ANYTHING IN THIS PACKAGE.

These files were copied from the v1-frozen git tag on 2026-04-22 and are
the reference implementation of the v1 rubric. They exist to let the
admin run "what would v1 have scored this song?" comparisons against the
evolving v2 calibrator. Any change to this package invalidates the
control.

If you need to update v1, roll a new frozen snapshot (e.g. v1_1_frozen/)
instead.
"""

RUBRIC_COMMIT = "a839df9"
